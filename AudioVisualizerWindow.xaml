<Window x:Class="GlassBar.AudioVisualizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GlassBar audio visualizer" Width="360" Height="132"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown"
        AutomationProperties.AutomationId="AudioVisualizerWindow">
  <Window.ContextMenu>
    <ContextMenu Background="#F2111722" BorderBrush="#48FFFFFF" Foreground="#F8FAFC">
      <MenuItem Header="Close visualizer" Foreground="#F8FAFC" Click="CloseVisualizer_Click" />
    </ContextMenu>
  </Window.ContextMenu>
  <Grid Background="#01000000" Cursor="SizeAll">
    <Border Margin="5" CornerRadius="22" Background="#080B1220" BorderBrush="#18FFFFFF" BorderThickness="1">
      <Border.Effect><DropShadowEffect BlurRadius="22" ShadowDepth="0" Opacity="0.38" Color="#38BDF8" /></Border.Effect>
      <Grid Margin="18,14">
        <ItemsControl x:Name="Bars">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate><UniformGrid Rows="1" /></ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
        </ItemsControl>
        <TextBlock Text="AUDIO" HorizontalAlignment="Right" VerticalAlignment="Top"
                   Foreground="#52FFFFFF" FontSize="8" FontWeight="SemiBold" IsHitTestVisible="False" />
      </Grid>
    </Border>
  </Grid>
</Window>
